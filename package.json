{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "build": "pnpm --filter @workspace/api-server build && pnpm --filter @workspace/nova-era-admin build"
    "start": "pnpm --filter @workspace/api-server start",
    "dev": "pnpm -r --parallel run dev"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1",
    "tsx": "^4.20.6"
  }
}
